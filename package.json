{
  "name": "verba",
  "module": "index.ts",
  "type": "module",
  "private": false,
  "workspaces": [
    "core",
    "builtins/*"
  ],
  "scripts": {
    "dev": "bun --elide-lines=0 --no-summary  --filter @verba/core dev",
    "build": "bun --elide-lines=0 --no-summary --filter @verba/core build",
    "start": "bun --elide-lines=0 --no-summary --filter  @verba/core start --",
    "test": "bun test"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild-visualizer": "^0.7.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@citation-js/core": "^0.7.21",
    "@citation-js/plugin-bibtex": "^0.7.21"
  }
}
