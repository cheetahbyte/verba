{
  "name": "verba-parser",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile verba",
    "dev": "bun run ./src/index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1",
    "zod": "^4.1.1"
  }
}
